# Simple Railway Python App

A minimal Flask application ready for Railway deployment.

## Deployment

1. Push this folder to GitHub.
2. Create a new project on Railway from GitHub repo.
3. Railway will auto-detect code.
4. **Start Command:** `gunicorn app:app` (Defined in Procfile)
